# Economics Hub — Dependencies
# Install: pip install -r requirements.txt

# Core
pandas>=2.0.0

# Data Sources
yfinance>=0.2.36
fredapi>=0.5.1

# Charts (for PNG newsletter generation — still used alongside Streamlit)
matplotlib>=3.7.0
numpy>=1.24.0

#other
python-dotenv>=1.0.0
